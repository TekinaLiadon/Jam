{
  "username" : "varchar(20)",
  "password": "varchar(64)",
  "email": "varchar(20)",
  "role": "varchar(20)",
  "hash": "varchar(64)",
  "blacklist": "boolean"
}
// CREATE TABLE global (id int NOT NULL AUTO_INCREMENT, username varchar(20) NOT NULL UNIQUE, password varchar(64), role varchar(20), access_token VARCHAR (30), refresh_token VARCHAR (30), PRIMARY KEY(id) )
// CREATE TABLE Lukih (id INT, role varchar(20), blacklist boolean, FOREIGN KEY (id) REFERENCES global(id), PRIMARY KEY(id))

/* CREATE TABLE sub_info (
    id INT,
    email varchar(20),
   blacklist boolean,
   FOREIGN KEY (id) REFERENCES global(id), PRIMARY KEY(id)
) */

// ALTER TABLE `Lukih` ADD newid INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY FIRST;